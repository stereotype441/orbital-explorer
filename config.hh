#ifndef CONFIG_HH
#define CONFIG_HH

// The highest atomic number (Z value) allowed. Since the program is
// for educational use, we try to maintain this value at the
// highest-numbered element that's been discovered.

const int MAX_ATOMIC_NUMBER = 118;

#endif
